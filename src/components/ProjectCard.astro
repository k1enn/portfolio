---
import { RiArrowRightUpLine } from "react-icons/ri";

export interface Project {
  title: string;
  description: string;
  image: string;
  link: string;
  tags?: string[];
  year?: string;
  stack?: string;
}

export interface Props {
  project: Project;
  index?: number;
}

const { project, index = 0 } = Astro.props;
const number = String(index + 1).padStart(2, "0");

const isVariant = project.image.startsWith("/images/") && project.image.endsWith(".webp");
const base = isVariant ? project.image.slice(0, -".webp".length) : "";
const srcset = isVariant
  ? `${base}-400w.webp 400w, ${base}-800w.webp 800w, ${project.image} 1280w`
  : undefined;
const sizes = srcset ? "(min-width: 768px) 50vw, 200px" : undefined;
const displaySrc = isVariant ? `${base}-400w.webp` : project.image;
---

<a
  class="project-card"
  href={project.link}
  target="_blank"
  rel="noopener noreferrer"
  aria-label={`${project.title} — open on GitHub (new tab)`}
>
  <div class="project-card-inner">
    <div class="project-thumb-wrap">
      <img
        class="project-thumb"
        src={displaySrc}
        srcset={srcset}
        sizes={sizes}
        alt={project.title ? `${project.title} preview` : ""}
        loading="lazy"
        decoding="async"
        width="400"
        height="225"
      />
      <span class="project-meta">{number}</span>
    </div>

    <div class="project-body">
      <div class="project-head">
        <h3 class="project-title">{project.title}</h3>
        <span class="project-arrow" aria-hidden="true">
          <RiArrowRightUpLine size={22} />
        </span>
      </div>

      {
        project.tags && project.tags.length > 0 && (
          <div class="project-tags">
            {project.tags.map((tag) => (
              <span class="project-tag">{tag}</span>
            ))}
          </div>
        )
      }

      <p class="project-description">{project.description}</p>
    </div>
  </div>
</a>

<style>
  .project-card {
    display: block;
    height: 100%;
    color: var(--fg);
    text-decoration: none;
  }
  .project-card:focus-visible { outline: none; }
  .project-card-inner {
    background: var(--surface);
    border: 2px solid var(--border);
    height: 100%;
    display: flex;
    flex-direction: column;
    color: var(--fg);
    transition: background 0.15s ease;
  }
  .project-card:hover .project-card-inner,
  .project-card:focus-visible .project-card-inner {
    background: var(--surface-hover);
  }
  .project-thumb-wrap {
    position: relative;
    width: 100%;
    padding-top: 56.25%;
    overflow: hidden;
    border-bottom: 1px solid var(--line);
    background: var(--surface);
  }
  .project-thumb {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .project-meta {
    position: absolute;
    top: 0;
    left: 0;
    background: var(--surface);
    color: var(--fg);
    border-right: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
    padding: 4px 12px;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.04em;
  }
  .project-body {
    display: flex;
    flex-direction: column;
    gap: 12px;
    padding: 16px;
    flex: 1;
  }
  .project-head {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 12px;
  }
  .project-title {
    font-family: var(--font-display);
    font-size: 1.25rem;
    line-height: 1.1;
    letter-spacing: -0.02em;
    word-break: break-word;
    margin: 0;
    font-weight: 700;
  }
  .project-arrow {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    min-width: 40px;
    border: 2px solid currentColor;
    transition: background 0.15s ease, color 0.15s ease;
  }
  .project-card:hover .project-arrow,
  .project-card:focus-visible .project-arrow {
    background: var(--fg);
    color: var(--bg);
  }
  .project-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  .project-tag {
    font-size: 0.76rem;
    font-weight: 500;
    letter-spacing: 0.02em;
    border: 1px solid var(--muted);
    color: var(--muted);
    padding: 4px 9px;
  }
  .project-description {
    font-size: 0.95rem;
    line-height: 1.5;
  }
  @media (min-width: 640px) {
    .project-body { padding: 20px; }
    .project-title { font-size: 1.5rem; }
  }
</style>
